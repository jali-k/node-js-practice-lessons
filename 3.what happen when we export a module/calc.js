const num1 = 5;
const num2 = 6;

function addThem(){
    console.log(`The sum is: ${num1 + num2}`);
}

addThem() // We call the function inside the module

// We have not exported anything from this module.